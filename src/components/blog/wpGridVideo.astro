---
import Item from '~/components/blog/wpGridItemVideo.astro';
import type { Post } from '~/types';

export interface Props {
  posts: Array<Post>;
}

const { posts } = Astro.props;
---

<div class="grid gap-6 row-gap-5 md:grid-cols-2 lg:grid-cols-2 mr-20 ml-20 ">
  {posts.map((post) => <Item post={post} />)}
</div>
